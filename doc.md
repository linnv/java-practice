### build build.gradle
```
gradle init
```

### add gradlew
```
gradle wrapper

```

### build and run app with gradlew
```
./gradlew build && java -jar build/libs/msg-spring-boot-docker-1.0.0.jar
```
